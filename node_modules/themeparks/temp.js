const themeparks = require("./lib/");

const texas = new themeparks.Parks.SixFlagsFiestaTexas({
    timeFormat: "dddd, MMMM Do YYYY, h:mm:ss a"
});

console.log(`${texas.DateNow({dateFormat:"dddd, MMMM Do YYYY, h:mm:ss a"})}`);

texas.GetOpeningTimes().then(data => {
    console.log(data[0]);
});